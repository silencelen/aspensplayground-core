# Aspen's Playground - Environment Configuration
# Copy this file to .env and customize for your deployment

# ===========================================
# SERVER CONFIGURATION
# ===========================================

# Port to run the server on (default: 3000)
PORT=3000

# Domain name for SSL certificate generation
# Used for Let's Encrypt paths and self-signed cert CN
DOMAIN=aspensplayground.com

# ===========================================
# LOGGING
# ===========================================

# Log level: DEBUG, INFO, WARN, ERROR
# DEBUG = verbose game events (spawns, pickups, etc.)
# INFO = general server events
# WARN = potential issues
# ERROR = errors only
LOG_LEVEL=INFO

# ===========================================
# SSL CERTIFICATES (Optional)
# ===========================================

# If not set, server will try:
# 1. Let's Encrypt paths (/etc/letsencrypt/live/DOMAIN/)
# 2. Self-signed certificates (auto-generated in ./ssl/)

# Custom SSL certificate paths (optional)
# SSL_KEY_PATH=/path/to/privkey.pem
# SSL_CERT_PATH=/path/to/fullchain.pem

# ===========================================
# PRODUCTION RECOMMENDATIONS
# ===========================================

# For production deployment:
# 1. Set LOG_LEVEL=WARN or LOG_LEVEL=ERROR
# 2. Use proper SSL certificates (Let's Encrypt recommended)
# 3. Run behind a reverse proxy (nginx/caddy) for additional security
# 4. Use a process manager (PM2, systemd) for auto-restart
